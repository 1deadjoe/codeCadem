# my_string = 'camperbot'
# camper = my_string[0:6]
# cp = my_string[0:6:3] #[x:y:h] -->Where x is the starting index, y is the ending index, and h is the step (the amount of characters to skip over).
# print(cp)

my_string = 'camperbot'
camperbot = my_string[::-1] #Reverses the whole string
print(camperbot)

#LUHN'S ALGORITHM
